.container
  .card.card-container.p-3.demo-account
    以下のアドレスとパスワードでログイン可能です
    %br
    %br
    .font-weight-bold
      test_user@example.com
      %br
      password
  .card.card-container
    = image_tag "logo.png", size: "200x55", class: "mx-auto"
    = link_to  '/auth/twitter', class: "btn btn-twitter mt-4" do
      = icon("fab", "twitter", class: "mr-1")
      %span Twitterでログイン
    .divide-text-login または
    = form_for(:session, url: login_path) do |f|
      = f.email_field :email, required: true, class: "form-control placeholder mt-4 mb-4", placeholder: "メールアドレス", value: "#{@email}"

      = f.password_field :password, required: true, class: "form-control placeholder mb-3", placeholder: "パスワード"

      = f.label :remember_me, class: "checkbox inline mb-4" do
        = f.check_box :remember_me
        %span.ml-1.remember-text 次回から自動でログインする

      = f.submit "ログイン", class: "btn btn-success btn-block mt-2"
      = link_to "パスワードをお忘れの場合", new_password_reset_path, class: "invite-text"
      = link_to "アカウントを持っていない場合", signup_path, class: "invite-text"
